# Session Scope: Tiles-to-Pyramid Extension

This session is focused on **qupath-extension-tiles-to-pyramid** -- stitching operations for acquired images.

## Scope

- Primary work directory: `qupath-extension-tiles-to-pyramid/`
- Language: Java 21, Gradle build system

## Guardrails

- Do NOT modify files in other subprojects unless explicitly asked
- Confirm with the user before touching other extensions or Python packages
- Git operations apply to THIS repository only -- verify with `pwd` and `git status` before committing
